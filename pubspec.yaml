name: covid_sa
description: All your latest stats and news for Covid19 in South Africa

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0
  provider: ^4.0.4
  equatable: ^1.0.0
  google_fonts: ^1.0.0
  flutter_bloc: ^4.0.0
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - config/
